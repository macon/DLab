<UserControl x:Class="DLab.Views.FolderSpecView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Left" VerticalAlignment="Center">
            <CheckBox Name="Subdirectory"/>
            <Label>Subfolder?</Label>
        </WrapPanel>

        <WrapPanel VerticalAlignment="Center" DockPanel.Dock="Left">
            <Label>Extensions:</Label>
            <TextBox Text="{Binding Extensions, UpdateSourceTrigger=LostFocus}" MaxLines="10" Width="150" VerticalContentAlignment="Center"/>
        </WrapPanel>
    </DockPanel>
</UserControl>
    
